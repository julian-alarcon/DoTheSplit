# Go
/api/bin/
/api/api
/api/worker
/api/coverage.out
*.test
*.out

# Node / Vue SPA
node_modules/
/frontend/dist/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local Docker Compose overrides
docker-compose.override.yml

# Generated code (uncomment if you want to keep generated out of VCS)
# /api/internal/apigen/*.gen.go
# /frontend/src/lib/api/schema.d.ts
# (the embedded SPA bundle under api/internal/webui/dist/ has its own .gitignore)

# SBOM artifacts (regenerated via `make sbom`, published per release)
/sbom/
