# Built SPA bundle is copied in at image-build time (make embed-frontend); never
# commit it. Keep this .gitignore and the .gitkeep so the embed dir exists and
# //go:embed stays valid in a fresh checkout.
*
!.gitignore
!.gitkeep
